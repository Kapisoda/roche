//import Vue from 'vue'
//import VueCookie from 'vue-cookie'

//Vue.use(VueCookie);

/*var CookieService = {
  setAuthToken (content, expires) {
    content = JSON.stringify(content)

    cookieJar.$cookie.set('idp-auth', content, {expires: expires})
  },
  getAuthToken () {
    return JSON.parse(cookieJar.$cookie.get('idp-auth')) || false
  },

}
export default CookieService*/
